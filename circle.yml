machine:
  node:
    version: 6.8.0

dependencies:
  pre:
    - npm config set spin false
    - npm install -g npm@^3
    - npm install -g bower

  override:
    - npm install
    - bower install

test:
  override:
    - ./node_modules/ember-cli/bin/ember test
